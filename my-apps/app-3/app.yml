apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-app-3
  labels:
    app: nginx-app-3
spec:
  selector:
    matchLabels:
  replicas: 2
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: nginx-app-3
    spec:
      containers:
        - name: nginx-app-3
          image: nginx
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
